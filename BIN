==== dynamic homepage
<script setup>
definePageMeta({
    layout: "plain",
});

// daftar semua konten, termasuk info kategori, subcategory, dan artikel
const daftarKategori = await queryContent("/").where({ info: true }).find();
</script>

<template>
    <div>
        <div class="con min-h-screen flex flex-col md:pt-24">
            <h1 class="text-5xl font-bold text-center mt-8">Tempat belajar Javascript paling oke 👍🏻</h1>

            <div class="flex flex-col gap-6 mt-12 bg-red-50">
                <template v-for="kategori in daftarKategori" :key="kategori._id">
                    <NuxtLink :to="kategori.artikelPertamaPath" class="hover:translate-y-1 border-2 border-gray-100 rounded mx-auto">
                        <div class="p-4 bg-base-200 rounded max-w-xl">
                            <h2 class="text-4xl mb-2">{{ kategori.title }}</h2>
``
                            <div class="btn btn-sm mr-2 btn-primary text-gray-100">{{ kategori.totalArtikel }} Artikel</div>

                            <div class="btn btn-sm btn-primary text-gray-100">{{ kategori.totalProyek }} Proyek</div>
                        </div>
                    </NuxtLink>
                </template>
            </div>
        </div>
    </div>
</template>
===

